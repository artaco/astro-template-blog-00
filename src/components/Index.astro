---
const {} = Astro.props
---
<nav class="my-3 p-5 border border-solid border-teal-700">
  <p>目次</p>
  <ul>
    <!-- {items.map((item) => (
      <li><a href={item.url}>{item.name}</a></li>
    ))} -->
  </ul>
</nav>
